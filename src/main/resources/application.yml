spring:
  jackson:
    time-zone: Asia/Ho_Chi_Minh
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=10m
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
  threads:
    virtual:
      enabled: true

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:englishai}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:12345}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000

  jpa:
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 25
          time_zone: Asia/Ho_Chi_Minh

  flyway:
    enabled: true
    clean-disabled: false
    locations: classpath:db/migration
    repair-on-migrate: true
    validate-on-migrate: false

  session:
    store-type: none # Use standard in-memory servlet container session for local dev

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

server:
  port: ${SERVER_PORT:8081}
  error:
    whitelabel:
      enabled: false
  servlet:
    session:
      timeout: ${SESSION_TIMEOUT:30m}

# Gitlawb Opengateway configuration
openai:
  api:
    key: ${OPENGATEWAY_API_KEY:ogw_live_f17bac25931451bf590dd1a52700c239}
    url: https://opengateway.gitlawb.com/v1

# Logging configuration
logging:
  level:
    root: INFO
    com.englishai: INFO
    org.hibernate.SQL: WARN
    org.hibernate.orm.results: WARN

# Application-level security properties
app:
  security:
    # Remember-me signing key; override via APP_SECURITY_REMEMBER_ME_KEY in production
    remember-me-key: ${APP_SECURITY_REMEMBER_ME_KEY:mentorAIDefaultKey-changeMeInProd}

# Rate limiting using Bucket4j starter
bucket4j:
  enabled: true
  filters:
    - cache-name: buckets
      filter-method: servlet
      url: /api/.*
      rate-limits:
        - bandwidths:
            - capacity: 60
              time: 1
              unit: minutes
              refill-speed: greedy
